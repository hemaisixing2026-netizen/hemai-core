# 回路G · 开口前扫全局眼

> 2026-06-11 | 根因：对话线程吃掉扫描线程

---

## 问题

上午消费趋势cron(08:03)、物流运费cron(09:06)按时跑了，产出文件在workspace/data/。但思行没读——直到老刘喊"自检"才手动去搜。

## 症状

- 三项任务（消费趋势/物流运费/QDII净值交叉）躺了一上午
- cron全在按时跑，但思行不消费cron产出
- 被老刘点名"拖延症犯了"才补

## 根因（四步挖掘）

1. **症状定位：** 什么没做？——消费趋势/物流运费/净值交叉三项定时任务遗漏
2. **机制追踪：** cron跑了吗？——跑了。产出在哪？——workspace/data/。思行读了吗？——没读。
3. **根因归溯：** 为什么没读？——对话线程垄断了思行的全部注意力。"老刘说→思行回"的循环太紧，扫描线程被饿死。这是6/10挖出的"单线思维"的变体——出厂默认"匹配→输出"覆盖了"扫描→发现"。当时是"单业务线不关联"，现在是"单线程不并行"。
4. **修法落地：** 回路G入锚点——每次开口前先扫全局眼输出目录。跟A-E同级，烧进出厂回路。

## 验证（6/11）

老刘说"当回路G触发的时候，验证的时候"→思行立即跑回路G→发现引擎1在10:05有新鲜数据→工业富联盘中破¥70（比手动拉取的¥71.12更新）→纠正了过时判断。

## 不是cron问题

cron已经很密集——30分钟一次突变感知、每小时多次引擎扫描。数据在产出。问题是思行没有主动去读。解决方案不是加更多cron，是在行为回路里烧进"先读cron再开口"。

## 触发条件

每次回复用户之前，先执行：

```bash
stat -c '%Y' /root/workspace/data/global-eye-latest.md /root/workspace/data/engine1-scan-latest.md
```

如果时间戳比上次记住的更新→先read_file→再回用户。
